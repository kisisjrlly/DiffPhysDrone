Use case: scientific-educational
Task type: edit
Asset type: final SCI journal method figure for "Differentiable Active-Depth Model"

Primary request:
Make a tiny final correction to the right-side output wiring of the provided v16 figure. Preserve everything else.

Only correction:
- Add a clean black arrow/branch from the straight-through mask M_ST box to the observed depth D_obs box.
- Keep the existing D_raw (fixed depth) input arrow into D_obs.
- The D_obs box must visibly receive two inputs: D_raw and M_ST, consistent with "D_obs = D_raw x M_ST".
- Keep the existing M_ST branch to observed quality Q_obs.
- Keep the existing Q (quality) input arrow into Q_obs.
- Do not remove or change the Q -> M -> M_ST chain.

Do not change:
- Left input stack.
- S_phi rows and thumbnails.
- quality Q, soft validity M, straight-through mask M_ST boxes.
- Equations and labels.

Strict exclusions:
- No red arrows.
- No gradients.
- No loss box.
- No new formulas.
- No major layout changes.
