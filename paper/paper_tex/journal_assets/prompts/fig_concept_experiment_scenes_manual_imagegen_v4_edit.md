Use case: scientific-educational
Task type: edit
Asset type: final small correction to SCI experiment-scene figure
Primary request: Keep the current improved figure almost unchanged. Only restore the missing Glare bottom label and adjust the middle-row drone orientation cue.

Edit target invariants:
- Preserve the current top plan-view map exactly: fixed-x front wall with two vertical wall blocks, central slit, solid back wall, Start/Goal, slot guides, x/y axis.
- Preserve all panel borders, title tabs, colors, and the improved bottom sensor degradation visuals.
- Preserve the Glare/Specular/Dark column layout.
- Do not redraw the whole figure or alter the top map geometry.

Required small corrections:
1. Restore the missing text label in the bottom-left Glare sensor panel. In the blank right side of the orange dashed box, add exactly:
   "washed-out
   cue"
   Use black text, matching the style and size of the other bottom labels.

2. In the three middle-row scene panels, make the drone read as flying toward the wall/slit:
   - Keep the drone in the same bottom-center position.
   - The blue dashed arrows must continue pointing upward into the scene.
   - If possible, reduce the impression that the camera lens faces the viewer; show the drone slightly from behind/top-back or show the front sensor visually aligned toward the slit.
   - Do not distort the drone shape or remove it.

Avoid:
- Do not change the corrected top plan-view map.
- Do not remove the "false depth" or "weak return" labels.
- Do not make sensor-observation slit edges sharp again.
- Do not introduce new text besides the restored "washed-out cue" label.
